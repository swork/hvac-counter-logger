# Not really a requirements file, just a note about what external
# modules are needed via "mip install" if ./manifest.py isn't
# frozen into the micropython build.
#
# Note you'll also need secrets/wifi.py and secrets/couchdb.py.
# Samples are at https://github.com/swork/public-secrets.git
#
aiohttp
datetime
os
time
unittest
